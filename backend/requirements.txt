fastapi==0.111.0
uvicorn==0.30.1
chromadb==0.5.20
langchain==0.2.11
langchain-community==0.2.10
langchain-openai==0.1.20
# Imported directly by rag/ingestor.py. It only arrived transitively through langchain
# before, which works until langchain's dependency tree shifts and re-ingestion breaks for
# no visible reason. Pinned explicitly because we import it ourselves.
langchain-text-splitters==0.2.4
openai==1.37.1
fastembed==0.8.0
pyspellchecker==0.9.0
better-profanity==0.7.0
slowapi==0.1.10
requests==2.32.3
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.8.2
pytest==8.2.2

# Deliberately absent: `anthropic` and `google-generativeai`. llm/client.py imports both
# INSIDE their provider functions, so they are only needed if LLM_PROVIDER is set to
# "anthropic" or "gemini". Production runs openai, and keeping them out keeps the image
# smaller. Add the matching package if you ever switch provider.
